# 数据集流水线：位置说明与给后端的问题反馈

> 数据组 → 后端负责人 / 知识图谱负责人 / RAG 负责人
> 代码基准：`github.com/LoveLearnLearning/esa` @ `42c2864`（2026-08-11 复核，`tool_schemas.json` 规范化指纹 `e62713fb`，22 个工具）
>
> ⚠️ 指纹有两种算法，别对错：`be4469f2…` 是文件**原始字节** sha256，`e62713fb` 是**规范化 JSON** sha256（我们 `schema_version` 用的）。同一个文件。
> 更新：2026-08-11

## 数据集流水线在哪

**就在本仓库的 `dataset/`。** 本仓库是唯一主仓库。

曾经短暂存在过一个独立仓库 `chenweihang0160-alt/esa-dataset`，那是并入本仓库之前的中转，
**PR 合并后即下线**，不要再往那边推东西，也不要拿它当参考 —— 两个源迟早会漂。

## 这份文件是干什么的

**后端仓库里我们一个字都没改。** 下面每条都标了 `文件:行号`，
要改的话由你们在自己仓库里改；改完告诉我们一声，我们重跑生成器对齐数据。

排序按「对数据集和 demo 的影响」，不是按修起来的难易。

---

## P0-1　工具调用格式两边不兼容，而且失败是静默的

| | |
|---|---|
| 现状 | `parse_output` 只认 `<tool_call><function=X><parameter=K>v</parameter></function></tool_call>` |
| 出处 | `backend/core/utils/parser.py`（复刻版在 `dataset/esa/backend_parser.py:45-70`） |
| 冲突 | LLaMA-Factory 0.9.4 的**所有** `qwen*` 模板产出 `<tool_call>{"name":…,"arguments":{…}}</tool_call>`（`data/tool_utils.py:423-428`），没有任何 `tool_format` 能产出 XML 那种形状 |

**最麻烦的是失败方式**：`func_match` 匹配不到就 `continue`，最后返回一个空的 `ParsedOutput` ——
前端显示空白，**无异常、无日志**。跑 `python3 dataset/tests/test_parser_compat.py` 有完整实证。

XML 格式在 `backend/tests/test_parser.py` 里有两个专门的测试，说明是刻意设计不是疏忽。
所以我们的建议不是让你们改后端，而是**给 LLaMA-Factory 写一个自定义 `tool_format` 复刻你们的 XML**。
两条路都行，需要拍板走哪条：

- **A. 后端 parser 兼容两种格式**：改动最小，`dataset/esa/backend_parser.py:73-107` 有可直接用的修复版，
  而且和 vLLM `--tool-call-parser hermes` 天然一致。
- **B. 给 LLaMA-Factory 写自定义 tool_format**：后端零改动，但训练侧多一个需要长期维护的补丁。

**这件事不阻塞我们造数据**（ShareGPT 文件本身格式无关），但阻塞训练配置和部署。

---

## P0-2　同一个 Agent 里有三套「失败」表示，模型得同时学三种

这是我们这次补 `tool_error` 数据时才完整摸清楚的。目前线上一个工具失败，可能长成三种完全不同的样子：

> **2026-08-11 复核：这条仍然成立，三种形态都还在。**
> 行号已按当前 `main`（42c2864）更新，观测示例也改成了 JSON（P0-3 已修，见下）。
> 另外发现第四处 `allowed: False`：`learning_tools.py:220`，原清单里没有。

| 形态 | 观测长什么样 | 出处（42c2864 实测行号） |
|---|---|---|
| ① 抛异常被包装 | 字符串 `"[Error]: 搜索请求超时"` | `agent/tools/tool_register.py:76,82` |
| ② 正常返回带 `error` 键 | `{"expression": "1/0", "result": null, "error": "除零错误"}` | `calculator.py:150,159`、`bitwise_calculator.py:162`、`math_solver.py:219,303` |
| ③ 正常返回但被拒绝 | `{"allowed": false, …, "reason": "当前会话为 isolated 模式…"}` | `learning_tools.py:220`、`memory_tools.py:240,292`、`mastery_tools.py:69` |

三个计算器**从不抛异常**，所以永远走不到 ①；其余工具则永远走不到 ②。

**建议**：统一成一种。我们倾向都走 ②（返回体里带 `error`），因为它保留了结构化上下文
（`expression` / `operation` 还在），比一个字符串好消费。

**我们已经如实按三种形态各造了数据**（`dataset/seeds/tool_errors.yaml`，每条带你们的行号），
所以这条不阻塞我们。但统一之后模型会好学很多，也少一类线上翻车的可能。

---

## ~~P0-3　`observation` 用 `str(result)` 而不是 `json.dumps`~~　✅ 已于 2026-08-11 修复

```python
result_text = str(result)     # agent.py:252 和 agent.py:351，两处
```

对 dict 而言 `str()` 是 **Python repr**：单引号、`True`/`False`/`None`，
而不是 JSON 的双引号和 `true`/`false`/`null`。

Qwen 的工具调用能力是在 **JSON 工具结果**上训出来的，喂 Python repr 等于让它解析一个陌生格式。

**建议**改成：

```python
result_text = json.dumps(result, ensure_ascii=False)
```

~~我们目前是按你们现状生成的（`OBSERVATION_FORMAT = "repr"`）。你们改了之后，
我们把那个开关切成 `"json"` 重跑一遍就行。~~

**✅ 你们已经改了**：`serialize_tool_result` 在 `backend/agent/agent.py:48-54`，
用的是 `json.dumps(result, ensure_ascii=False, default=str)`。
我们已把 `render.py` 的 `OBSERVATION_FORMAT` 切成 `"json"` 并全量重生成（1,117 条）。

⚠️ 顺带提醒一个**很容易漏的连带影响**，我们自己差点踩：
返回**字符串**的工具（`load_skill` / `get_time` / `get_weather`，以及全部
`[Error]: …` 失败观测）现在会被 JSON 编码，多出一对双引号 ——
`str("北京: 26 摄氏度 晴朗")` 是裸串，`json.dumps(...)` 是 `"北京: 26 摄氏度 晴朗"`。
我们已按真实输出对齐，并加了黄金样例把它钉住
（`dataset/tools/capture_math_outputs.py` 的 `OBSERVATION_PROBES`）。

---

## P1-1　`get_time` 有两个 bug

```python
return datetime.now(timezone.utc).strftime("%D-%H:%M:%S")   # agent/tools/tools.py:46-49
```

1. **返回 UTC，而学生在东八区**。学生问「今天几号」「还有几天考试」，在每天 08:00 之前会**差一天**。
2. **`%D` 展开成 `%m/%d/%y`** ——「08/10/26」这种美式两位年份有歧义（是 2026 年 8 月 10 日还是 2010 年？），
   而且 `%D` 是 glibc 扩展，不保证可移植。

**建议**：`datetime.now(ZoneInfo("Asia/Shanghai")).strftime("%Y-%m-%d %H:%M:%S")`。

数据里我们已如实复刻现状（`dataset/esa/fixtures.py:531-540`）。你们改了我们重生成那 5 条即可。

---

## P1-2　`get_weather` 是硬编码桩

```python
def get_weather(city: str) -> str:
    return f"{city}: 26 摄氏度 晴朗"     # agent/tools/tools.py:28-29
```

恒返回同一句。后果：这个工具**只能训「什么时候该调它」，训不了「怎么消费真实天气」**。

考虑到它在教育 Agent 里本来就是边缘工具，我们给的量已经压到最少。
**不接真实天气 API 也可以，但请在 demo 里避开它** —— 评审现场问一句「北京天气」返回
「26 摄氏度 晴朗」，赶上下雨就很尴尬。

---

## P1-3　并行工具调用的消息结构，训练侧对不上

线上对并行调用是**每个结果单独一条 `tool` 消息**：

```python
for tool_call in tool_calls:      # agent.py:246 和 agent.py:345
    ...
    result_text = str(result)
```

而 ShareGPT 的角色顺序是强制交替的（`data/converter.py:144-177`），一条 `function_call`
后面只能跟**一条** `observation`，多个结果只能合并进同一条。

**所以我们目前一条并行调用样本都没生成**（`dataset/data/ir/` 里 `parallel_tool_call` 类是 0 条）。
需要你们确认：并行调用在 demo 里到底用不用？如果用，我们需要知道合并成一条之后，
模型在真实推理时收到的分成多条的观测还能不能对上。

---

## ~~P1-5　`parse_output` 的 `_try_cast` 丢类型：字符串 `"0"` 会变成整数 `0`~~　✅ 已于 2026-08-11 修复

`_try_cast` 用 `json.loads` 还原参数值（`backend/core/utils/parser.py`），
所以 XML 里的 `<parameter=lower>0</parameter>` 一律得到整数 `0` ——
即使 schema 明确把这个参数声明成字符串。

具体踩到的一处：`math_solver` 的 `lower` / `upper` / `point` 在 schema 里是 `"type": "string"`
（`math_tools/math_solver.py` 的注册块），因为它们要能装下 `n`、`oo` 这类符号表达式。
模型正确地写出 `lower="0"`，经 `_try_cast` 之后工具收到的是 `0`。

**影响**：

- 对 `math_solver` 本身是良性的 —— `sympify(0)` 和 `sympify("0")` 结果一样。
- 但它**破坏了工具自己的 schema 契约**，任何依赖参数类型的工具都会中招。
- 对我们的评测更直接：同一个模型，走 XML 解析器时参数匹配率和 schema 合法率会莫名掉几个点，
  走 JSON 解析器时又是满分 —— 差的那几分测的是解析器，不是模型。
  我们已在 `dataset/esa/eval.py` 的 `args_equal` / `coerce_to_schema` 里做了归一，
  **但那是判分侧的补偿，线上的类型丢失还在。**

~~**建议**：`_try_cast` 参照 schema 里声明的类型来转，而不是无条件 `json.loads`。~~

**✅ 你们已经改了，而且比我们建议的更彻底**：新增 `backend/core/utils/tool_arguments.py`
的 `normalize_tool_arguments`，按 schema 恢复 string/boolean/integer/number/array/object
六种类型并校验 enum；`parser.py:79-99` 在抽参数时就先看声明类型，
声明 `string` 的直接取原文不再过 `json.loads`。掰不动时 `except ValueError: pass`
原样保留交给执行边界 —— 这个取舍我们认同，一次坏参数不该让整轮请求变成 500。

我们这边已同步：`dataset/esa/backend_parser.py` 跟上了新逻辑，并新增
`dataset/tools/capture_parser_golden.py` + `tests/test_parser_compat.py`，
拿你们真实的 `parse_output` 跑 1,163 条输入做逐条比对（当前全部一致）。
以后你们再改解析器，我们这边会直接红给我们看，而不是等评测分数莫名对不上。

---

## P2-1　`tool_register.call` 只捕获三种异常

```python
except (ValueError, TypeError, RuntimeError) as e:    # tool_register.py:79-80
    return f"[Error]: {e}"
```

其余异常会直接穿过去。目前已知一个具体例子：

```python
bitwise_calculator("1 << 10000000")
# ValueError: Exceeds the limit (4300 digits) for integer string conversion
```

这个 `ValueError` 是在 `_format_result` 里 `bin()` 转换时抛的，**逃出了 `bitwise_calculator`
自己的 try**（`bitwise_calculator.py:156-162` 只裹了 `safe_evaluate`），于是它不会变成
形态 ② 的 `{'error': …}`，而是变成形态 ① 的 `[Error]: …`。同一个工具两种失败长相。

`read_file` 那个 `raise NotImplementedError`（`agent/tools/read_file.py:43`）也不在捕获范围内，
不过它没注册进 22 个工具，暂时打不到。

**建议**：`except Exception` 兜底，或者至少把 `_format_result` 也放进 try。

---

## P2-2　`get_mastery_report` 课程名写错时静悄悄返回空报告

`mastery_store.get_report`（`mastery_store.py:369-405`）→ `kg_store.get_course_points`
（`knowledge_graph.py:266-286`，一条普通 SQL）查不到就返回空列表，于是报告是
`total_points=0 / avg_mastery=0.0 / weak_points=[]` 的空壳，**没有任何提示**。

对比一下 `recommend_practice`：同样是课程名查不到，它会带一句
`note: "未找到课程 'xxx' 的知识点，请确认课程名"`（`mastery_tools.py:161-168`）。

同一件事两种处理，模型只能从其中一个学到「课程名可能写错了」。
**建议给 `get_mastery_report` 也补上同样的 `note`。**

（我们数据里已按现状复刻：`dataset/esa/fixtures.py` 的 `get_mastery_report` 返回空报告不抛错。）

---

## P2-3　`retrieve_knowledge` / `get_knowledge_base_stats` 拿不到真实输出，覆盖率卡在 20/22

这两个工具目前**一条正例都没有**，因为我们没有真实观测值：

| 工具 | 能不能造 | 为什么 |
|---|---|---|
| `retrieve_knowledge` | **不能** | 返回的是语料库里检索出的**文档片段 + 来源标注**（`rag/agent_api.py:48-74`）。不知道索引了哪些文档就编，等于伪造引用出处，而「内容可追溯」是赛题明写的评分项 |
| `get_knowledge_base_stats` | **只差一次真实输出** | 返回的是**系统配置**不是文档内容：`collection_id` / `document_count` / `total_chunks` / `embedding_model` / `reranker_model` / `index_backend` / `config`（`rag/agent_api.py:77-91）` |

**请RAG 负责人在配好检索服务的环境里跑一次这两行，把输出贴给我们**，一分钟的事：

```python
from backend.agent.tools.rag_tool import get_knowledge_base_stats
print(get_knowledge_base_stats())
```

拿到之后 `get_knowledge_base_stats` 的正例立刻能补上，工具覆盖率 20/22 → 21/22。

`retrieve_knowledge` 需要一份真实检索导出（几条 query 的完整返回即可），
在那之前我们只做负样本和参数约束样本。

---

## P1-4　`DATASET_GENERATION.md` 里有三处会直接踩坑

这份文档写得很好，方案本身我们基本照做了（程序化生成场景、真实执行工具、
自动校验清单、按模板整组切分防泄漏）。但有三处和实际情况对不上，都会造成实打实的损失：

**① `template: qwen3_5` 这个模板不存在，训练会直接报错**

文档第一节写「LLaMA-Factory 会根据 `template: qwen3_5` 把结构化数据转换成 Qwen3.5
原生工具调用文本」。我们在 LLaMA-Factory 0.9.4 源码里 grep 过，**`qwen3_5` 零处命中**，
实际有的是：

```
qwen / qwen2_audio / qwen2_omni / qwen2_vl / qwen3 / qwen3_nothink
qwen3_omni / qwen3_omni_nothink / qwen3_vl / qwen3_vl_nothink
```

应当用 **`qwen3`**，而且不能用 `qwen3_nothink` —— 后者不产出 `<think>`，
你们的 `StreamOutputParser` 会把整个回答当 reasoning 吐出、正文为空。

**② 示例里的 observation 结构是错的，我们照抄踩了坑**

文档第四节的工具调用样本写着：

```json
{ "from": "observation", "value": "{\"value\":5.0}" }
```

而 `calculator` 真实返回的是 `{"expression": "...", "result": 5}`
（`math_tools/calculator.py:167-170`）—— 没有 `value` 这个字段。

我们照着这个示例造了 63 条样本（calculator 21 / bitwise 20 / math_solver 22），
全部在教模型消费一个线上不存在的结构，而所有校验都是绿的。已经改成
`import` 你们的真实函数执行取值了，但**建议把文档里的示例也更正**，
不然下一个照做的人会再踩一遍。

同一节 `save_core_memory` 的 `{"success":true}` 也是错的，真实是
`{"saved": ..., "memory_key": ..., "content": ..., "category": ..., "profile_projection": ...}`。

**③ 顺带确认一件好事：基座型号写着 `Qwen/Qwen3.5-122B-A10B`**

按 Qwen 的命名惯例，`A10B` 是「激活 10B 参数」，也就是 **MoE 而非 dense**。
我们此前一直标记为「未知」，显存和吞吐规划卡在这。如果确实是 MoE，
8×A800 的部署方案要按 MoE 来配。开训前建议还是 `cat <模型路径>/config.json` 确认一次
（看 `architectures` 里有没有 `Moe`、有没有 `num_experts`）。

## 附：我们已经如实复刻、不需要你们改的

这些不是问题，只是记录一下我们按什么造的数据，方便你们改代码时知道会波及什么：

| 行为 | 出处 | 我们怎么处理的 |
|---|---|---|
| `loop_times = 3`（最多三轮工具调用） | `agent.py:88` | 样本最长链式调用不超过三轮 |
| `max_model_len=32768` / `max_output_tokens=8192` | `agent.py:93-94` | 校验器 `--cutoff-len` 默认 8192 |
| system prompt 七段拼装，约 1300 字符 | `core/message/build_prompt.py` | 逐段复刻在 `dataset/esa/system_prompt.py` |
| `# 可用 Skills` 索引注入 system prompt | `build_prompt.py:80` | 所以 `load_skill` 的 `name` **不需要 enum**，模型看得到有哪些技能 |
| `load_skill` 失败返回 `"{name} skill not found!"`（不带 `[Error]:`） | `agent/tools/skills.py:249` | 已按原样造了 3 条 404 样本 |
| 优先级公式 `0.35*mastery_need + 0.20*review_pressure + 0.20*weight + 0.15*exam_urgency + 0.10*prerequisite_risk` | `mastery_store.py:407+` | 逐字复刻在 `fixtures.py`，`test_fixture_contract.py` 钉住 |
| `retention = 2^(-days/stability_days)`、`REVIEW_THRESHOLD = 0.65` | `learning/student_model.py` | 同上 |
| 三个计算器的真实返回结构 | `math_tools/*.py` | 由 `dataset/tools/capture_math_outputs.py` **执行你们的真实函数**抓取，落在 `dataset/data/cache/math_real.json` |

**只要这些行为有改动，请知会数据组** —— 我们的数据是按它们生成的，改了不通知，
训练数据和线上就会悄悄分家，而且不会有任何报错。
